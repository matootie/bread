# AWS CDK Stack

This package handles creation and deployment of the AWS CDK stack.

It also has a redirect Lambda used to route all requests under `/api` to Lambda and all other requests to the single-page app hosted on S3.
